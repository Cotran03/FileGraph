# FileGraph config

이 폴더는 로컬 실행 설정을 위한 자리입니다.

- `settings.json`은 로컬 런타임 상태이므로 Git에 커밋하지 않습니다.
- API 키와 비밀값은 커밋하지 않습니다.
- `SPEC.md` 기준으로 API 키는 평문 설정 파일이 아니라 `keyring`에 저장합니다.
- 이후 설정 예시 파일이 필요하면 실제 값 없이 `*.example.*` 형식으로 추가합니다.
