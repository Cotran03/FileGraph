# FileGraph 첫 릴리즈 체크리스트

이 체크리스트는 AI 기능이 없는 첫 릴리즈 후보를 검증하기 위한 기준입니다.

## 1. 릴리즈 범위 고정

- [v] 현재 브랜치가 `main`인지 확인한다.
- [v] AI 기능, API 키 입력, AI provider, AI 정책 문서, AI 의존성이 포함되지 않았는지 확인한다.
- [v] 새 기능 추가를 멈추고 버그 수정과 문서 정리만 반영한다.
- [v] `README.md`, `SPEC.md`, `USAGE.md`가 현재 UI와 기능을 설명하는지 확인한다.

## 2. 자동 검증

- [v] `.venv\Scripts\python.exe -m pytest`가 통과한다.
- [v] 테스트 후 새로 생긴 불필요한 캐시나 임시 파일을 정리한다.
- [v] 빌드 전에 `git status --short`로 의도한 변경만 남았는지 확인한다.

## 3. 빌드 검증

- [v] `.venv\Scripts\pyinstaller.exe FileGraph.spec --noconfirm`으로 빌드한다.
- [v] `dist\FileGraph\FileGraph.exe`가 생성되는지 확인한다.
- [v] 빌드 산출물에 `assets/`와 `db/template_db.db`가 포함되는지 확인한다.
- [v] 빌드 중 생성된 불필요한 `build/`, `__pycache__/`, `.pytest_cache/`는 정리한다.

## 4. 첫 실행 QA

- [v] 깨끗한 런타임 데이터 상태에서 실행한다.
- [v] `%LOCALAPPDATA%/FileGraph/database.db`가 자동 생성되는지 확인한다.
- [v] 빈 그래프 화면이 깨지지 않고 표시되는지 확인한다.
- [v] 한글 경로와 공백이 있는 경로에서 실행 문제가 없는지 확인한다.
- [ ] 작은 창 높이에서 작업 패널/컨트롤 바가 겹치지 않고 스크롤로 접근되는지 확인한다.

## 5. 기본 기능 QA

- [v] 파일 추가가 동작한다.
- [v] 폴더 추가가 동작한다.
- [v] 폴더 내부 파일 자동 등록 미리보기가 표시된다.
- [v] 하위 파일 등록 취소가 동작한다.
- [v] 그래프 영역 드래그 앤 드롭 등록이 동작한다.
- [v] 중복 경로 추가 시 기존 노드 이동, 관계 추가, 취소 흐름이 동작한다.
- [?] 샘플 데이터 생성이 동작한다.

## 6. 그래프 조작 QA

- [v] 노드 드래그 후 위치가 저장된다.
- [v] 자동 정렬이 동작한다.
- [v] `Ctrl+Z`로 자동 정렬을 되돌릴 수 있다.
- [v] 오른쪽 드래그 다중 선택이 동작한다.
- [v] 선택 노드 일괄 삭제가 동작한다.
- [v] 노드 삭제는 원본 파일을 삭제하지 않고 앱 안에서만 숨긴다.
- [v] 노드 더블클릭으로 원본 파일 또는 폴더가 열린다.

## 7. 관계 QA

- [v] 관계 추가가 동작한다.
- [v] 관계 수정이 동작한다.
- [v] 관계 삭제가 동작한다.
- [v] 방향성 관계에 화살표가 표시된다.
- [v] 관계 강도에 따라 간선 두께가 달라진다.
- [v] 관계 타입 색상 변경이 그래프에 반영된다.

## 8. 보기와 검색 QA

- [ ] 검색창 검색이 동작한다.
- [ ] 검색 제안 선택이 동작한다.
- [ ] 선택 노드 포커스 보기가 동작한다.
- [ ] 전체 보기로 돌아갈 수 있다.
- [ ] 보기 프리셋 `누락 파일만`이 동작한다.
- [ ] 보기 프리셋 `강조 노드 주변`이 동작한다.
- [ ] 보기 프리셋 `최근 추가`가 동작한다.
- [ ] 보기 프리셋 `선택 폴더 아래`가 동작한다.
- [ ] 보기 프리셋 `고립 노드`가 동작한다.

## 9. 설정 QA

- [ ] 라벨 글자 크기 설정이 저장된다.
- [ ] 노드 라벨 표시 방식이 저장된다.
- [ ] 간선 라벨 표시 방식이 저장된다.
- [ ] 파일/폴더 노드 색상 설정이 그래프에 반영된다.
- [ ] 색상 미리보기 칩이 입력값을 반영한다.
- [ ] 기본 색상 초기화가 동작한다.
- [ ] 강조 색상 슬롯이 우클릭 강조 메뉴에 반영된다.
- [ ] 확장자 아이콘 매핑 추가/삭제가 동작한다.
- [ ] 무시 폴더 설정이 폴더 등록과 누락 찾기에 반영된다.

## 10. 파일 상태 QA

- [ ] 파일 위치 갱신이 `ACTIVE`, `MISSING`, `ACCESS_DENIED`를 반영한다.
- [ ] 파일 위치 갱신 진행 창에 처리 개수와 현재 항목이 표시된다.
- [ ] 파일 위치 갱신 취소가 동작한다.
- [ ] 활성 파일의 SHA-256 해시가 저장된다.
- [ ] 100MB 초과 파일은 자동 해시 계산에서 제외된다.
- [ ] 누락 파일 찾기가 저장된 해시로 파일을 복구한다.
- [ ] 누락 파일 찾기 진행 창에 검사 개수와 현재 항목이 표시된다.
- [ ] 누락 파일 찾기 취소가 동작한다.

## 11. DB와 내보내기 QA

- [ ] DB 백업이 동작한다.
- [ ] DB 가져오기 전 현재 DB가 `.preimport-...db`로 자동 백업된다.
- [ ] FileGraph DB가 아닌 파일은 가져오기를 거부한다.
- [ ] 정상 DB 가져오기 후 그래프와 설정이 다시 로드된다.
- [ ] JSON 내보내기가 동작한다.
- [ ] CSV 내보내기가 동작한다.

## 12. 릴리즈 마감

- [x] `git status --short`에 의도한 소스/문서 변경만 있는지 확인한다.
- [x] 릴리즈 커밋을 만든다.
- [x] `origin/main`에 푸시한다.
- [x] `main` 병합 전 마지막으로 AI 관련 파일/문구/의존성이 없는지 확인한다.
- [ ] 릴리즈 태그와 릴리즈 노트 초안을 준비한다.

## 점검 기록

- 2026-07-09: `main`에서 자동 검증 완료. `pytest` 132개 통과.
- 2026-07-09: 빌드 산출물 `dist\FileGraph\FileGraph.exe` 확인.
- 2026-07-09: 빌드 산출물 내부 `_internal\assets`와 `_internal\db\template_db.db` 확인.
- 2026-07-09: OpenAI/API key/LLM/provider 관련 코드 흔적 검색 결과 없음.
- 2026-07-09: `%LOCALAPPDATA%\FileGraph`가 존재함. 첫 실행 QA를 다시 하려면 런타임 데이터를 삭제한 뒤 실행해야 한다.
- 2026-07-09: 작업 패널/컨트롤 바가 작은 창 높이에서 빽빽해 UI가 겹칠 수 있음. 스크롤 가능한 패널로 보강 필요.

